namespace _3DNS
{
    /// <summary>
    /// Enum for 3DNS outcome codes
    /// </summary>
    public enum Outcome
    {
        Failure = -1,
        SuccessNoChange = 0,
        SuccessWithChange = 1
    }
}